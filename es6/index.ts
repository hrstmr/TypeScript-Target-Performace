import { job } from './data';

const world = 'world';

// default argument
function hello(who: string = world): string {
    return `Hello ${who}! `;
}

if (job.tags?.export_id) {
}

if (job.tags?.export_id?.includes('8848c5d8-fd44-43c0-b7f2-1428d0f4891a')) {
}
